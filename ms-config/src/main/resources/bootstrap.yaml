server:
  port: 8084
spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/hector-gallard0/por-mingeso
          search-paths: config-files